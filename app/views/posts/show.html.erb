<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>
<%= link_to "Edit", edit_post_path(@post), :class => 'btn btn-outline-info' %>
<%= link_to "Delete", edit_post_path(@post), 
					method: :delete,
					data: {confirm: "Are you shure"},
					:class => 'btn btn-outline-danger' %>